name: aura_vhc
description: "AURA Virtual Health Companion - Stage 2 Build"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  # Firebase Core for initialization
  firebase_core: ^3.10.1

  # Cloud Firestore for health data and test messages
  cloud_firestore: ^5.6.2

  # Firebase Auth for the 2-step verification requirement
  firebase_auth: ^5.4.4

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true